spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/SpringCloud_Sell?useSSL=false&serverTimezone=UTC
    username: root
    password: 1997
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: update
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
